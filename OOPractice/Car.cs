using System;

namespace OOPractice
{
    public class Car : Vehicle
    {
        public Car(string name, int speed) : base(name, speed)
        {
        }

/*        public Car(string name, int speed, string driverName) : base(name, speed)
        {
        }*/
    }
}